{
    "buildCommand": "pnpm turbo run build --filter=@repo/api --filter=web...",
    "installCommand": "pnpm install --frozen-lockfile",
    "outputDirectory": "apps/web/.next",
    "framework": "nextjs"
}